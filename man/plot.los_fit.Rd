% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_los.R
\name{plot.los_fit}
\alias{plot.los_fit}
\title{Plot a fitted length of stay survival curve}
\usage{
\method{plot}{los_fit}(x, ...)
}
\arguments{
\item{x}{A \code{los_fit} object from \code{\link[=fit_los]{fit_los()}}.}

\item{...}{Unused, present for S3 generic consistency.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders the recovered survival curve \code{P(LOS > d)} for
\verb{d = 0..max_stay} as a step line, with the family and fitted
parameters shown as a subtitle. Useful for sanity checking a
\code{\link[=fit_los]{fit_los()}} result before passing it to \code{\link[=fcast_census]{fcast_census()}}.
}
\examples{
set.seed(1)
true_surv <- spec_los("negbin", mu = 3, k = 2, max_stay = 30)
admissions <- rpois(120, 20)
census <- stats::convolve(admissions, rev(true_surv), type = "open")[
  seq_along(admissions)
]
fit <- fit_los(
  data.frame(admissions = admissions, census = census),
  family = "negbin", max_stay = 30
)
plot(fit)
}
