% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_los.R
\name{spec_los}
\alias{spec_los}
\title{Specify a length of stay survival vector from a parametric family}
\usage{
spec_los(family, ..., max_stay = 50)
}
\arguments{
\item{family}{Character. One of \code{"negbin"}, \code{"normal"},
\code{"lognormal"}, \code{"geometric"}.}

\item{...}{Parameters for the chosen family (see above).}

\item{max_stay}{Integer. Longest LOS modelled, in time step units
of the forecast (weeks for FluSight, days for daily hubs). The
returned vector has length \code{max_stay + 1}.}
}
\value{
Numeric vector of length \code{max_stay + 1} giving
\code{P(LOS > d)} for \verb{d = 0..max_stay}.
}
\description{
Returns \code{P(LOS > d)} for \verb{d = 0, 1, ..., max_stay}. Use this when
you do not have observed census data to fit on and want to plug in
literature priors. The returned numeric vector drops straight into
\code{\link[=fcast_census]{fcast_census()}}. Counterpart of \code{\link[=fit_los]{fit_los()}}, which recovers the
same vector from data.
}
\details{
Parameters per family:
\itemize{
\item \code{negbin}: \code{mu} (mean), \code{k} (dispersion).
\item \code{normal}: \code{mu} (mean), \code{sigma} (sd).
\item \code{lognormal}: \code{meanlog}, \code{sdlog}.
\item \code{geometric}: \code{mu} (mean).
}
}
\examples{
spec_los("negbin", mu = 2, k = 1.7, max_stay = 15)
}
