% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_los.R
\name{fit_los}
\alias{fit_los}
\title{Fit a length of stay survival curve from observed admissions and census}
\usage{
fit_los(data, family = "negbin", max_stay = 50, skip = max_stay)
}
\arguments{
\item{data}{Data frame with one row per time step and two numeric
columns: \code{admissions} (observed admissions at that time step) and
\code{census} (observed census at that time step).}

\item{family}{Character. LOS distribution family to fit. One of
\code{"negbin"}, \code{"normal"}, \code{"lognormal"}, \code{"geometric"} (same set as
\code{\link[=spec_los]{spec_los()}}). Default \code{"negbin"}.}

\item{max_stay}{Integer. Longest LOS modelled, in time step units
of the data. The returned survival vector has length
\code{max_stay + 1}. Default 50.}

\item{skip}{Integer. Number of time steps at the start of the
series to ignore in the loss. Their census depends on admissions
before the observation window, which we do not see. Default
\code{max_stay}.}
}
\value{
A numeric vector of length \code{max_stay + 1} giving
\code{P(LOS > d)} for \verb{d = 0..max_stay}, of class \code{los_fit}. Drops
directly into \code{\link[=fcast_census]{fcast_census()}}. Fitted parameters and family are
attached as attributes.
}
\description{
Recovers \code{P(LOS > d)} by deconvolving an observed admissions and
census time series. Use this when you have last season's admissions
and census time series and want a data driven LOS for
\code{\link[=fcast_census]{fcast_census()}}. If you do not have such data, use \code{\link[=spec_los]{spec_los()}}
with literature priors instead.
}
\details{
If instead you have a line list (one observed length of stay per
patient), you do not need \code{fit_los()}. Build the empirical survival
from the vector directly and pass it to \code{\link[=fcast_census]{fcast_census()}}:
\code{surv <- 1 - stats::ecdf(los_values)(0:max_stay)}.
}
\examples{
set.seed(1)
true_surv <- spec_los("negbin", mu = 3, k = 2, max_stay = 30)
admissions <- rpois(120, 20)
census <- stats::convolve(admissions, rev(true_surv), type = "open")[
  seq_along(admissions)
]
fit <- fit_los(
  data.frame(admissions = admissions, census = census),
  family = "negbin", max_stay = 30
)
attr(fit, "params")
}
